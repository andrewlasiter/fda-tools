---
description: "Alias: pipeline — run full end-to-end 510(k) analysis pipeline (research → validate → draft → assemble)"
allowed-tools: Bash, Read, Glob, Grep, Write, AskUserQuestion, WebFetch, WebSearch
argument-hint: "<product-code> --project NAME [--device-description TEXT] [--intended-use TEXT] [--years RANGE] [--full-auto]"
---

> **Alias** — This is a short alias for the `pipeline` command.
> Full documentation: [pipeline.md](pipeline.md)
> You can use `/fda-tools:pipeline` and `/fda-tools:p` interchangeably.

<!-- Pass all arguments through to pipeline -->
