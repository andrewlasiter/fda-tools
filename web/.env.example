# MDRP Web — Environment Variables
# Copy to .env.local and fill in your values.
# NEVER commit .env.local to source control.

# ── Supabase (browser-safe public keys) ─────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ── FastAPI / Bridge server URL ──────────────────────────────────────
# Cloud SaaS: set to your FastAPI service URL (e.g. https://api.yourdomain.com)
# Local development: bridge server default is http://localhost:18790
NEXT_PUBLIC_API_URL=http://localhost:18790

# ── Bridge API key (development / self-hosted only) ──────────────────
# Required when the bridge server uses X-API-Key auth directly (local dev).
# Cloud deployments authenticate via Supabase JWT instead — leave blank.
# NEVER commit a real key here. Use .env.local for real values.
NEXT_PUBLIC_BRIDGE_API_KEY=

# ── App config ──────────────────────────────────────────────────────
NEXT_PUBLIC_APP_ENV=development
